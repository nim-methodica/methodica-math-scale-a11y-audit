# דוח נגישות מאחד — methodica-math-scale-01 (4 מודולים)
**תקן:** WCAG 2.1 AA + ת"י 5568 · **תאריך:** 2026-06-24
**שיטה:** scan.py + axe-core 4.10.2 על **כל מסך בכל מודול** (ניווט `goTo` — כיסוי מלא, לא מדגם) + ניגודיות מחושבת לכל מסך + אינוונטר תמונות/רכיבים מלא + אימות-מקלדת.

> דוחות פר-מודול: [01-01](accessibility-report-01-01.md) · [01-02](accessibility-report-01-02.md) · [01-03](accessibility-report-01-03.md) · [01-04](accessibility-report-01-04.md)

## סיכום חומרות
| מודול | מסכים נסרקו | 🔴 | 🟠 | 🔵 |
|---|:--:|:--:|:--:|:--:|
| 01-01 (לימוד + תרגול) | 24/24 | 3 | 5 | 3 |
| 01-02 (תרגול בסיסי) | 9/9 | 1 | 3 | 2 |
| 01-03 (משימת כיתה) | 3/3 | 1 | 2 | 3 |
| 01-04 (תרגול מתקדם) | 18/18 | 3 | 3 | 2 |

## חוסמים (🔴) — לפי מודול
| מודול | בדיקה | תופעה | מיקום |
|---|---|---|---|
| 01-04 | #33 גרירה ללא חלופת-מקלדת | תרגיל drag-and-drop (מסך 3/s39) — 7 כרטיסים + 4 אזורים ללא מקלדת/ARIA. **משתמש מקלדת/קורא-מסך אינו יכול לענות כלל** | `index.html:289–359` |
| 01-04 | #8 GIF משוב מונפש | משוב נכון/שגוי כ-GIF מונפש ללא בקרת-עצירה | `script.js:1448` |
| 01-01 | #23 ניגודיות | טקסט-תשובות `#aeaeae` על לבן = **2.22:1**; טקסט `#303030` על בועה סגולה `#a51cff` = **2.65:1** | מסכים 7–14 |
| כל המודולים | #28 שדות ללא תווית | שדות-תשובה `<input>` ללא `aria-label` — axe critical | 01-01: 1093,1211,1442 · 01-02: 464,744 · 01-03: 80 · 01-04: 444,643,756 |

## אזהרות חוצות-מודולים (🟠)
| בדיקה | תופעה | חל על | תיקון |
|---|---|---|---|
| #13 חיווי-פוקוס חלש | פוקוס נשען על `border-color:#a51cff75` שקוף, לא `:focus-visible` בולט אחיד (קיים תקין רק ב-`flag-btn`/כפתורי-דיווח) | כל המודולים | אחדו `:focus-visible{outline:3px solid #7100B9;outline-offset:2px}` |
| #26 reflow/הגדלה | קנבס קבוע 1280×710 מתכווץ כיחידה (scale) במקום שהתוכן ישתקף; טקסט זעיר במובייל/בזום | כל המודולים | אפשרו reflow / ודאו זום-טקסט 200% במנגנון-ההטמעה |
| #3 כותרת H1 | אין `<h1>` (01-02, 01-04) · כפול (01-03) | 02/03/04 | `<h1>` יחיד לכל דף |

## ליטוש (🔵) — בכל המודולים
- **#4** — אין `<main>` (עטפו את אזור-התוכן). · **#30** — אין `@media (prefers-reduced-motion)`.

## מה אומת תקין (בכל המסכים, בכל המודולים)
`lang="he" dir="rtl"` · `<title>` · **כל התמונות עם `alt`** (דקורטיביות עם `alt=""` כראוי) ·
**ניגודיות תקינה בכל המסכים של 02/03/04** (טקסט כהה על לבן) · הפעלת-מקלדת לכרטיסי-בחירה (Enter/Space) ·
`aria-checked` מתעדכן · axe ללא הפרות פרט לשדות-הקלט.

## ⚠️ תיקון מהותי לדיווח הקודם (שהיה מדגמי)
הבדיקה הראשונה הסתמכה על **הצלבת design-tokens ב-CSS** והניחה שבעיות-ניגודיות חוצות את כל המודולים.
**הבדיקה המקיפה (כל מסך בריצה חיה) הפריכה זאת:** קומפוננטות הבועה-הסגולה והתשובות-הבהירות
מתרנדרות **רק במודול 01-01**; ב-02/03/04 הטקסט כהה-על-לבן ועובר. כמו כן נמצאו בבדיקה המקיפה
שני ממצאים שהוחמצו במדגם: **תרגיל-הגרירה (01-04)** ו**GIF-המשוב המונפש (01-04)** — שניהם מוזרקים
בזמן-ריצה ולכן לא נתפסו בסריקה הסטטית או בהחלפת-class.

## פתרונות מפורטים — קוד (תיקונים גלובליים, חלים על כל המודולים)
לחצו על כל סעיף כדי לפתוח את פתרון-הקוד המלא. התיקונים מתאימים לקובץ-התבנית המשותף (`styles.css`/`index.html`).

:::solution 🔴 #28 — שדה קלט ללא תווית (הוסיפו aria-label)
לכל שדה-תשובה הוסיפו `aria-label` שמתאר מה להזין. דוגמה ל-`.s18-answer-box`:
```html
<!-- לפני -->
<input class="s18-answer-box" id="s18-answer-input" type="text" dir="rtl">

<!-- אחרי -->
<input class="s18-answer-box" id="s18-answer-input" type="text" dir="rtl"
       aria-label="הקלידו את התשובה המספרית">
```
אם יש כיתוב נראה לפני השדה (`<span id="s18-q">...</span>`), עדיף לקשר אליו:
```html
<input class="s18-answer-box" id="s18-answer-input" type="text" aria-labelledby="s18-q">
```
החילו על כל המופעים: 01-01 (1093,1211,1442) · 01-02 (464,744) · 01-03 (80) · 01-04 (444,643,756).
:::

:::solution 🟠 #13 — חיווי-פוקוס חלש (החליפו ב-:focus-visible אחיד)
כיום הרכיבים מסתמכים על `border-color:#a51cff75` (סגול שקוף) או `outline:none`. הוסיפו כלל גלובלי
שמבטיח חיווי-פוקוס בולט לכל רכיב הניתן למיקוד:
```css
/* תיקון גלובלי — להוסיף ל-styles.css */
*:focus-visible {
  outline: 3px solid #7100B9;   /* סגול כהה, ניגודיות ≥3:1 מול לבן */
  outline-offset: 2px;
  border-radius: 4px;
}
/* אם קיים outline:none על רכיבים אינטראקטיביים — להסירו או להחליפו בכלל לעיל */
.option-card:focus-visible,
.scale-input:focus-visible + .scale-marker {
  outline: 3px solid #7100B9;
  outline-offset: 2px;
}
```
**הימנעו** מ-`outline:none` ללא חלופה. `:focus-visible` מציג את החיווי רק בניווט-מקלדת (לא בעכבר),
כך שאין פגיעה בעיצוב למשתמשי-עכבר.
:::

:::solution 🟠 #26 — reflow/הגדלה (הקנבס הקבוע 1280×710)
המבנה הנוכחי מקבע קנבס ומקטין אותו כיחידה (`transform: scale`), כך שטקסט לא ניתן להגדלה עצמאית.
שתי גישות (לפי מורכבות שהצוות מוכן לקבל):
```css
/* גישה א' (מינימלית) — לאפשר זום-טקסט עד 200% בלי חיתוך, גם אם נשאר scale:
   ודאו שמיכל-העל אינו חוסם גלילה אנכית, והסירו max-height קשיח על תיבות-טקסט. */
#app { overflow: auto; }
.hook-card-inner, .s7-card-q, .s18-answer-box { max-height: none; }

/* גישה ב' (נכונה לטווח-ארוך) — פריסה גמישה במקום קנבס קבוע:
   החליפו רוחב/גובה קבועים ב-min()/clamp() ואפשרו לתוכן לרדת שורה (wrap). */
#app { width: min(1280px, 100vw); height: auto; aspect-ratio: auto; }
.s39-cols, .option-cards { flex-wrap: wrap; }
```
בנוסף ודאו שתגית ה-viewport אינה חוסמת זום (היא תקינה כיום). מומלצת גישה ב' לעמידה מלאה ב-1.4.10.
:::

:::solution 🔵 #4 + #30 — landmark ראשי ו-prefers-reduced-motion
```html
<!-- #4: עטפו את אזור-התוכן ב-<main> (במקום <div id="app">, או בתוכו) -->
<main id="app" aria-label="לומדה: קנה מידה">
  ...
</main>
```
```css
/* #30: הוסיפו לסוף styles.css — מכבד בקשת תנועה-מופחתת של המשתמש */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.001ms !important;
    scroll-behavior: auto !important;
  }
}
```
:::

## הסתייגות
axe-core + scan.py מכסים בעיות מכניות (~30-40%). מצבים שמאחורי אינטראקציה (הכרזת-משוב/live-regions,
חלונות-רמז, מלכודת-פוקוס במודל-הדיווח, ניתוב-פוקוס במעברי-מסך, צבעי נכון/שגוי) דורשים **בדיקה ידנית
עם קורא-מסך אנושי (NVDA בעברית / VoiceOver)**. דוח זה אינו תחליף להסמכת-נגישות רשמית.
