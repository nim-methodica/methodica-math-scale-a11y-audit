# דוח בקרת נגישות — קנה מידה (Scale) · מודול 01-03
**מודול:** methodica-math-scale-01-03 (משימת כיתה) · **קבצים:** index.html / script.js / styles.css · **תאריך:** 2026-06-24
**תקן:** WCAG 2.1 AA + ת"י 5568 · **שיטה:** scan.py + axe-core 4.10.2 על **כל 3 המסכים** (ניווט `goTo`) + ניגודיות מחושבת + אינוונטר מלא
**סיכום:** 🔴 1 חוסם · 🟠 2 אזהרות · 🔵 3 ליטוש

> **כיסוי:** נסרקו 3/3 המסכים בריצה חיה. אישור סופי דורש קורא-מסך אנושי.

## מה נמצא תקין (אומת חי)
- `<html lang="he" dir="rtl">` ✓ · כותרת ✓ · **2 תמונות — כולן עם `alt`, לא שבורות** ✓
- **ניגודיות תקינה בכל המסכים** (טקסט כהה על לבן) ✓ · אין גרירה אינטראקטיבית · אין GIF מונפש
- הקונפיגורציה `correctMap/revealMap` ב-script.js היא **תצוגת-פתרון** בלבד (אין `draggable`) — לא תרגיל-גרירה

## ממצאים
| סוג הבעיה | מיקום | מה נמצא (אומת חי) | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| 🔴 #28 שדה קלט ללא תווית | `index.html:80` (`.s35-input`, מסך 2) | שדה `<input>` ללא `<label>`/`aria-label` — axe critical | **הוסיפו** `aria-label` מתאר | קורא-מסך מזהה ייעוד השדה (WCAG 1.3.1/4.1.2 A) |
| 🟠 #13 חיווי-פוקוס חלש | `styles.css:141` + מופעי `outline:none`; פוקוס נשען על `border-color:#a51cff75` שקוף | אינו `:focus-visible` בולט אחיד | **אחדו** ל-`:focus-visible` בולט (כמו `flag-btn`) | פוקוס נראה (WCAG 2.4.7 AA) |
| 🟠 #26 reflow/הגדלה | קנבס קבוע 1280×710 | מתכווץ כיחידה ולא משתקף; טקסט זעיר במובייל/בזום | **אפשרו reflow** / ודאו זום 200% | קריאוּת בהגדלה (WCAG 1.4.4/1.4.10 AA) |
| 🔵 #3 ריבוי H1 | `index.html` | נמצאו **2** תגי `<h1>` (רצוי אחד) | **השאירו** `<h1>` יחיד; הורידו את השני ל-`<h2>` | היררכיית-כותרות (WCAG 1.3.1 A — best practice) |
| 🔵 #4 אין landmark ראשי | `index.html` | אין `<main>` | **עטפו** ב-`<main>` | ניווט-מהיר (best practice) |
| 🔵 #30 אין prefers-reduced-motion | `styles.css` | חסר בלוק תנועה-מופחתת | **הוסיפו** `@media (prefers-reduced-motion)` | רגישי-תנועה (AAA — מומלץ) |

## הערת-תיקון
דיווח קודם (מדגמי) טען לבעיית-ניגודיות סגולה — **הופרך** בבדיקה החיה: ניגודיות תקינה בכל המסכים.

## פתרונות מפורטים — קוד
כל ממצאי מודול זה (#28 שדה, #13 פוקוס, #26 reflow, #3 H1, #4 main, #30 reduced-motion) הם
**תיקונים גלובליים** עם קוד מלא בדוח הסיכום — ראו `accessibility-report-SUMMARY` › "פתרונות מפורטים".
דוגמה מהירה ל-#28 (שדה 80): הוסיפו `aria-label="הקלידו את התשובה"` ל-`<input class="s35-input">`.

## עדיפויות
1. 🔴 `aria-label` לשדה (80). 2. 🟠 חיווי-פוקוס, reflow. 3. 🔵 H1 יחיד + main + reduced-motion.
**צעד הבא:** בדיקת קורא-מסך — הכרזת-משוב, ניתוב-פוקוס.
