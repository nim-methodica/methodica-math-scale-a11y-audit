# דוח בקרת נגישות — קנה מידה (Scale) · מודול 01-02
**מודול:** methodica-math-scale-01-02 (תרגול בסיסי) · **קבצים:** index.html / script.js / styles.css · **תאריך:** 2026-06-24
**תקן:** WCAG 2.1 AA + ת"י 5568 · **שיטה:** scan.py + axe-core 4.10.2 על **כל 9 המסכים** (ניווט `goTo`) + ניגודיות מחושבת על כל מסך + אינוונטר תמונות/רכיבים מלא
**סיכום:** 🔴 1 חוסם · 🟠 3 אזהרות · 🔵 2 ליטוש

> **כיסוי:** נסרקו 9/9 המסכים בריצה חיה (לא מדגם). ניגודיות נמדדה על הרינדור בפועל של כל מסך.
> **מגבלה:** מצבים שמאחורי אינטראקציה (משוב נכון/שגוי, חלונות-רמז) לא תמיד נתפסו. אישור סופי דורש
> קורא-מסך אנושי (NVDA/VoiceOver).

## מה נמצא תקין (אומת חי על כל המסכים)
- `<html lang="he" dir="rtl">` ✓ · כותרת ✓
- **8 תמונות — כולן עם `alt`, אף אחת לא שבורה** ✓
- **ניגודיות תקינה בכל 9 המסכים** — טקסט `#303030`/`#222` על לבן (≈12:1) ✓ (אין כאן בועות-רמז סגולות)
- axe — ללא הפרות פרט לאמור למטה ✓ · אין תרגילי-גרירה · אין GIF מונפש

## ממצאים
| סוג הבעיה | מיקום | מה נמצא (אומת חי) | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| 🔴 #28 שדה קלט ללא תווית | `index.html:464, 744` (`.s18-answer-box`, מסכים 4,7) | 2 שדות `<input>` ללא `<label>`/`aria-label` — axe critical | **הוסיפו** `aria-label` מתאר לכל שדה | קורא-מסך מזהה ייעוד השדה (WCAG 1.3.1/4.1.2 A) |
| 🟠 #3 אין H1 | `index.html` | אין אף `<h1>` בדף | **הוסיפו** `<h1>` ראשית | היררכיית-תוכן (WCAG 1.3.1 A) |
| 🟠 #13 חיווי-פוקוס חלש | `styles.css:141` + 8 מופעי `outline:none`; פוקוס נשען על `border-color:#a51cff75` שקוף | רוב הרכיבים מסומנים בשינוי-border עדין, לא `:focus-visible` בולט (כפי שכן יש ב-`flag-btn`) | **אחדו** ל-`:focus-visible{outline:3px solid #7100B9;outline-offset:2px}` | פוקוס נראה בניגודיות ≥3:1 (WCAG 2.4.7 AA) |
| 🟠 #26 reflow/הגדלה | קנבס קבוע 1280×710 (`#app`) | התוכן מתכווץ כיחידה (scale) ולא משתקף; טקסט זעיר במובייל/בזום | **אפשרו reflow** / ודאו זום-טקסט 200% במנגנון-ההטמעה | קריאוּת בהגדלה (WCAG 1.4.4/1.4.10 AA) |
| 🔵 #4 אין landmark ראשי | `index.html` | אין `<main>`/`role=main` | **עטפו** ב-`<main>` | ניווט-מהיר לקורא-מסך (best practice) |
| 🔵 #30 אין prefers-reduced-motion | `styles.css` | חסר בלוק תנועה-מופחתת | **הוסיפו** `@media (prefers-reduced-motion: reduce)` | רגישי-תנועה (WCAG 2.3.3 AAA — מומלץ) |

## הערת-תיקון מהבדיקה המקיפה
דיווח קודם (מדגמי) טען לבעיית-ניגודיות של בועת-דמות סגולה גם כאן — **זה הופרך**: הבדיקה החיה על כל
9 המסכים מצאה ניגודיות תקינה. הקומפוננטה הסגולה קיימת ב-CSS אך **אינה מתרנדרת בזרימת מודול 02**.

## פתרונות מפורטים — קוד
כל ממצאי מודול זה (#28 שדות, #13 פוקוס, #26 reflow, #3 H1, #4 main, #30 reduced-motion) הם
**תיקונים גלובליים** עם קוד מלא בדוח הסיכום — ראו `accessibility-report-SUMMARY` › "פתרונות מפורטים".
דוגמה מהירה ל-#28 (השדות 464, 744): הוסיפו `aria-label="הקלידו את התשובה"` לכל `<input class="s18-answer-box">`.

## עדיפויות
1. 🔴 `aria-label` ל-2 השדות (464, 744). 2. 🟠 H1, חיווי-פוקוס, reflow. 3. 🔵 main + reduced-motion.
**צעד הבא:** בדיקת קורא-מסך — הכרזת-משוב, ניתוב-פוקוס במעברי-מסך, מצבי נכון/שגוי.
